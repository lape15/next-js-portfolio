import styles from "./navbar.module.css";
const Navbar = () => {
  return (
    <nav className={styles.head}>
      <span className={styles.span}>L.A</span>
    </nav>
  );
};

export default Navbar;
