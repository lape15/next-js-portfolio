import styles from "./navbar.module.css";
const Navbar = () => {
  return (
    <nav className={styles.head}>
      <span className={styles.span}>LAPE </span>
    </nav>
  );
};

export default Navbar;
